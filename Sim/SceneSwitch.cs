using System;
using Android.Views.Animations;
using Android.Animation;
namespace Sim
{
	public static class SceneSwitch
	{
		const int _duration = 500;
		public static void Show()
		{
			//float _fromX = GameView.Instance.mainHidth;
			//_fromX *= GameView.Instance.Factor;
			//ValueAnimator animator = ValueAnimator.OfFloat(_fromX, 0);
			//animator.SetDuration(_duration);
			//animator.SetInterpolator(new DecelerateInterpolator());
			//animator.Update += (sender, e) => pivot.x = (float)e.Animation.AnimatedValue;
			//animator.AnimationEnd += (sender, e) => OnShow();
			//animator.Start();
		}
	}
}
